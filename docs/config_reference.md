# Configuration Reference

Complete documentation of all configuration keys in `config.yaml`.

## Structure

Configuration is organized into sections:

- **capture**: Data source configuration
- **fusion**: Track fusion parameters
- **airspace**: Geofence and NOTAM settings
- **gui**: User interface settings
- **tiles**: Map tile sourcing options

## Example Configuration

See `config.yaml` in the repository root for a complete example.
